package edu.neo4j.workshop.socialnetwork.relations;

import org.neo4j.graphdb.RelationshipType;

/**
 * @author partyks
 */
public enum LaungageRelationship implements RelationshipType {
    A1, A2, B1, B2, C1, C2
}
